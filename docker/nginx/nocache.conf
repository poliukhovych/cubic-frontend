# Не кешувати runtime-конфіг і сам index.html
location ~* ^/cubic-frontend/(config\.js|index\.html)$ {
  add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0";
  try_files $uri =404;
}
